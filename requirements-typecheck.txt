# Pinned tooling for the mypy gate (see .github/workflows/CI.yml `type_check`).
#
# Only mypy is installed here -- deliberately NOT the project's runtime/dev
# dependencies. The gate relies on `ignore_missing_imports` so every third-party
# import collapses to `Any`, which keeps the mypy verdict reproducible regardless
# of which typed packages happen to be installed.
mypy==2.2.0
